---
import FeaturePageLayout from '../../layouts/FeaturePageLayout.astro';
---

<FeaturePageLayout
  title="Order Export — SPT"
  description="Route Shopify orders to Email, FTP/SFTP, or API destinations by SKU pattern, in the format your warehouse, ERP, or 3PL expects."
  eyebrow="Order export"
  headline="Route orders where you need: warehouse, ERP, or 3PL."
  subhead="As orders are placed, SPT can route them via Email, FTP/SFTP, or an HTTP API endpoint, by SKU pattern or brand, so different product lines can go to different suppliers — formatted to match the system on the other side. Background retries and a per-order processing log included."
  heroCaption="Admin destinations list + a sample exported file."
  howItWorks={[
    { step: 'Define destinations', body: 'One or more Email, FTP/SFTP, or HTTP API destinations, each with its own routing rule.' },
    { step: 'Map the payload',     body: 'A transformer maps Shopify order fields to your destination\'s format (CSV or XML).' },
    { step: 'Route by SKU',        body: 'Each rule matches line items by SKU pattern; a default rule catches everything else.' },
  ]}
  configurable={[
    'Destination type: Email, FTP/SFTP, or HTTP API',
    'Format: CSV or XML, via a per-destination transformer',
    'Routing rules matched by SKU pattern, with a default fallback rule',
    'Per-line-item routing — split a single order across destinations by SKU',
    'CC addresses and an admin copy of each export',
    'Automatic retries on delivery failure via background jobs',
  ]}
  dataSources="Independent of catalog and P&I. Use any combination of modules you need."
  faqs={[
    { q: 'Will it retry if delivery fails?', a: 'Yes. Delivery runs as a background job and retries automatically on failure.' },
    { q: 'Can I split one Shopify order across two destinations?', a: 'Yes. Routing rules match line items by SKU pattern, so different product lines on a single order can go to different suppliers.' },
    { q: 'Do you keep a record of what was sent?', a: 'Order processing is logged per order, including which rules matched and the outcome of each delivery.' },
    { q: 'Does this carry the in-cart fitment record?', a: 'The selected vehicle is visible on the order in your Shopify admin. Including it in the exported file isn\'t automatic — the transformer controls which fields are written, so we add it to a feed when a destination needs it.' },
    { q: 'Which tier includes this?', a: 'Order Export is part of the Pro tier ($300/month). The Free tier does not include outbound order export.' },
  ]}
/>
